nelmio_api_doc:
  documentation:
    info:
      title: 'Logopedia API'
      description: 'Rezerwacja spotkań logopedycznych'
      version: '1.0.0'
    components:
      securitySchemes:
        Bearer:
          type: http
          scheme: bearer
          bearerFormat: JWT
  areas:
    default:
      path_patterns: [ ^/api ]
