framework:
  rate_limiter:
    api_global:
      policy: 'token_bucket'
      limit: 100
      rate: { interval: '1 minute', amount: 100 }
    api_login:
      policy: 'sliding_window'
      limit: 5
      interval: '1 minute'
